{
  "name": "todo-jenkins-lab",
  "version": "1.0.0",
  "description": "Todo app simple pour s'entrainer a construire un pipeline Jenkins",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "lint": "eslint public server.js --no-eslintrc --env browser,node,es2021 --parser-options=ecmaVersion:2021 || true",
    "test": "node test/test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
